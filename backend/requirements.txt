# Phase 5 FastAPI Backend
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
email-validator==2.1.0
# ^ required at runtime by pydantic's EmailStr (used in phase5/api/schemas.py);
# without it, importing the schemas module raises ImportError. Was present
# in requirements-phase5.txt but missing here.
pydantic-settings==2.1.0
# NOTE: pydantic-extra-types==2.4.1 was listed here but requires
# pydantic>=2.5.2, which conflicts with the pydantic==2.5.0 pin above and
# made `pip install` unresolvable on a fresh checkout. It is not imported
# anywhere in phase5/, so it's removed rather than bumping pydantic (which
# would be a wider, untested change).

# Database
sqlalchemy==2.0.23
# PostgreSQL driver
# psycopg 3 binary extras provide prebuilt Windows/Linux/macOS wheels and
# avoid requiring local pg_config during pip installation.
psycopg[binary]==3.2.9
geoalchemy2==0.14.1
alembic==1.12.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.1
# ^ pinned explicitly: passlib 1.7.4 predates bcrypt's removal of the
# `__about__` attribute it uses for version detection. Left unpinned, pip
# installs bcrypt 5.x, which passlib's version-probe crashes on, and this
# also breaks hash/verify with "password cannot be longer than 72 bytes"
# even for normal passwords. bcrypt==4.1.1 avoids the crash and hashing
# works correctly (a harmless "(trapped) error reading bcrypt version"
# warning still prints, which is expected and non-fatal). Already pinned
# correctly in requirements-phase5.txt; was missing here.
python-multipart==0.0.6

# Security
cryptography==41.0.7

# Utilities
python-dateutil==2.8.2
pytz==2023.3
requests==2.31.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Development
python-dotenv==1.0.0
black==23.12.0
flake8==6.1.0
mypy==1.7.1

# Existing Phase 1-4C dependencies
# NOTE: previously pinned to exact old versions (pandas==2.0.3, numpy==1.24.3,
# etc.) which have no prebuilt wheels for Python 3.12, so a fresh `pip
# install -r phase5/requirements.txt` failed trying to build pandas from
# source (ModuleNotFoundError: pkg_resources). Relaxed to match the
# unpinned floors already used in the project's root requirements.txt.
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.12
scikit-learn>=1.3
scipy>=1.10
